namespace APITemplate.Constants
{
    public enum TaskStatuses
    {
        NotAssigned,
        Assigned,
        InProgress,
        Completed,
    }

    public enum UserRoles
    {
        User,
        Admin,
    }
}
